{
  "name": "mqtt-store-service",
  "version": "1.0.0",
  "description": "Service to store mqtt message on local mongodb instance and elaborate station stat.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kappu72@gmail.com",
  "license": "ISC",
  "dependencies": {
    "date-and-time": "^0.11.1",
    "mongodb": "^3.4.0",
    "mqtt": "^3.0.0",
    "rxjs": "^6.5.3"
  }
}
